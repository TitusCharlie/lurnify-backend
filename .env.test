# === PostgreSQL Config ===
PROJECT_NAME=LurnifyTest
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

DEBUG=True
# === App Secrets ===
SECRET_KEY=super-secret-key-used-for-jwt-signing

# === JWT Settings (Optional if handled in code defaults) ===
ACCESS_TOKEN_EXPIRE_MINUTES=60
ALGORITHM=HS256

# === Redis (for Celery) ===
REDIS_URL=redis://localhost:6379/0

# === IPFS / AWS / Supabase (Future use) ===
STORAGE_BACKEND=local  # Options: local, ipfs, s3, supabase
IPFS_API_URL=http://127.0.0.1:5001

# === Moralis / Web3 Auth (Placeholder, for phase 2) ===
WEB3_AUTH_PROVIDER_URL=https://rpc.example-chain.com
MORALIS_API_KEY=your-moralis-api-key
# DATABASE_URL=postgresql+psycopg2://postgres:yourpassword@localhost:5432/postgres
